# 电力图表快速创建功能说明

## 概述

在"快速创建" tab 中新增了电力行业专用的图表配置功能，支持快速配置部门、时间、指标、告警等业务参数。

## 功能模块

### 1. 📊 图表类型

**功能**：选择图表展示类型
- **折线图**：适用于展示负荷曲线、电压趋势等随时间变化的电力数据
- **饼图**：适用于展示用电量占比、能耗分布等比例关系
- **柱状图**：适用于对比各变电站、各部门的用电量大小

**交互**：点击按钮即可切换图表类型，自动更新数据和配置

---

### 2. 🏢 组织机构

**功能**：选择监测的部门或变电站

**选项**：
- 500kV变电站
  - 500kV站A
  - 500kV站B
- 220kV变电站
  - 220kV站C
  - 220kV站D
- 110kV变电站
  - 110kV站E
  - 110kV站F
- 用电部门
  - 生产车间
  - 办公区域
  - 照明系统
  - 空调系统
  - 其他设备

**特性**：
- 支持搜索（可输入关键词快速查找）
- 支持清空（不选择特定部门）
- 单选模式

---

### 3. ⏰ 时间维度

#### 3.1 统计周期

**功能**：选择数据统计的时间粒度

**选项**：
| 周期 | 说明 | 数据点数 |
|------|------|----------|
| 实时（秒级） | 每秒刷新，适用于实时监控 | 60个点 |
| 5分钟 | 每5分钟一个数据点 | 288个点 |
| 15分钟 | 每15分钟一个数据点 | 96个点 |
| 1小时 | 每小时一个数据点 | 24个点 |
| 1天 | 每天一个数据点 | 30个点 |
| 1月 | 每月一个数据点 | 12个点 |

**交互**：按钮组选择，默认为"1小时"

#### 3.2 自动刷新

**功能**：设置图表自动刷新频率

**选项**：
- 不自动刷新（手动刷新）
- 5秒
- 10秒
- 30秒
- 1分钟
- 5分钟
- 15分钟
- 30分钟

**特性**：
- 支持清空（不启用自动刷新）
- 下拉选择

**使用场景**：
- 实时监控：选择 5秒或10秒
- 定时更新：选择 1分钟或5分钟
- 长期监测：选择 15分钟或30分钟

---

### 4. 📈 监测指标

#### 4.1 选择指标

**功能**：选择要监测的电力指标

**选项**：
| 指标 | 单位 | 图标 | 说明 |
|------|------|------|------|
| 有功功率 | MW | ⚡ | 实际做功的功率 |
| 无功功率 | MVar | ⚡ | 在电路中起交换作用的功率 |
| 电压 | kV | 🎚️ | 电路中的电位差 |
| 电流 | A | 🔌 | 单位时间内通过的电荷量 |
| 功率因数 | cosφ | 💹 | 有功功率与视在功率的比值 |
| 用电量 | kWh | 💡 | 消耗的电能量 |

**特性**：
- 多选（可同时选择多个指标进行对比）
- 默认选中"有功功率"
- 使用图标增强可读性

#### 4.2 小数位数

**功能**：设置数据显示的小数位数

**配置**：
- 类型：数字输入
- 范围：0-6位
- 默认值：2位

**作用**：控制图表中数值显示的精度

---

### 5. ⚠️ 告警设置

**功能**：配置告警阈值规则

**配置项**：
1. **告警类型**
   - 上限告警：当数值超过阈值时触发
   - 下限告警：当数值低于阈值时触发

2. **阈值**
   - 数值输入框
   - 根据指标不同，单位也不同
   - 示例：
     - 有功功率：700 MW
     - 电压：235 kV

3. **监测指标**
   - 选择要告警的指标
   - 默认为"有功功率"

**特性**：
- 默认折叠（避免占用空间）
- 支持添加多条告警规则
- 组合表单形式

**使用场景**：
- 负荷监控：设置上下限告警
- 电压监控：电压过高/过低告警
- 预警机制：提前设置阈值，及时发现问题

---

### 6. 快速操作

#### 6.1 应用配置

**功能**：应用当前配置到图表

**操作**：点击主按钮，提交配置

#### 6.2 保存为模板

**功能**：将当前配置保存为模板，方便后续复用

**状态**：待实现（当前点击后输出日志）

**计划功能**：
- 保存当前所有配置
- 为模板命名
- 在"图表类型"旁边显示"快速模板"按钮组

#### 6.3 重置

**功能**：重置所有配置为默认值

**操作**：点击"重置"按钮

---

## 配置文件说明

### power-chart-config.ts

位置：`src/config/power-chart-config.ts`

**主要内容**：

1. **部门配置**
   - `departmentTree`: 部门树形结构
   - `departmentOptions`: 扁平化的部门列表（用于下拉选择）

2. **时间周期配置**
   - `timePeriodOptions`: 时间周期选项
   - `generateTimeLabels()`: 生成时间标签数组

3. **指标配置**
   - `metricOptions`: 监测指标列表
   - `getMetricById()`: 根据ID获取指标
   - `getMetricUnit()`: 获取单位

4. **告警配置**
   - `alarmThresholdOptions`: 告警阈值预设

5. **刷新配置**
   - `refreshIntervalOptions`: 刷新频率选项

---

## 使用示例

### 示例1：24小时负荷监测

**配置**：
- 图表类型：折线图
- 部门：500kV站A
- 时间周期：1小时
- 自动刷新：不启用
- 指标：有功功率

**效果**：显示该变电站24小时的负荷变化曲线

---

### 示例2：部门用电量对比

**配置**：
- 图表类型：饼图
- 指标：用电量
- 部门：生产车间、办公区域、照明系统

**效果**：环形饼图展示各部门用电量占比

---

### 示例3：多指标监控

**配置**：
- 图表类型：折线图
- 指标：有功功率、无功功率、电压
- 时间周期：5分钟
- 自动刷新：30秒
- 告警设置：
  - 上限 700 MW（有功功率）
  - 下限 215 kV（电压）

**效果**：多系列折线图，实时刷新，带告警线

---

## 数据流

```
用户配置 → 快速创建 Tab
    ↓
提取配置参数（deptId、timePeriod、metrics、interval、alarms）
    ↓
生成 API 请求配置
    ↓
更新图表 config（标题、单位、数据格式）
    ↓
刷新图表显示
```

---

## 后续扩展计划

### 短期（1-2周）
1. ✅ 基础配置界面
2. ✅ 部门/时间/指标选择
3. ✅ 告警配置
4. ⏳ 配置持久化（保存到 localStorage）
5. ⏳ 模板管理（保存/加载/删除模板）

### 中期（1个月）
1. ⏳ API 接口集成
2. ⏳ 实时数据刷新（WebSocket）
3. ⏳ 告警联动（邮件/短信/钉钉）
4. ⏳ 数据导出（Excel/PDF）

### 长期（2-3个月）
1. ⏳ 历史数据查询
2. ⏳ 数据对比分析
3. ⏳ 自定义报表
4. ⏳ 移动端适配

---

## 注意事项

1. **兼容性**：新配置项与原有的"属性" tab 配置并存
2. **优先级**：快速创建 tab 的配置优先级更高
3. **数据源**：当前使用模拟数据，需要对接实际 API
4. **性能**：自动刷新频率建议不低于5秒，避免频繁请求

---

## 技术支持

如有问题，请参考：
- [amis-editor 官方文档](https://aisuda.github.io/amis-editor-demo)
- [ECharts 配置手册](https://echarts.apache.org/zh/option.html)
- 项目 README.md
