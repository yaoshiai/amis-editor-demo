我想基于当前项目，实现一个amis-editor的重构定制开发.
技术上使用vite，react18，TS，pnpm,eslint,prettier等。
下面是已确定的需求:
你希望通过定制 amis-editor 来解决什么核心业务问题？这个编辑器主要是给谁使用的（比如：内部运营人员、开发者、客户等）？
内部运营人员。
这些运营人员主要会用这个编辑器来制作什么类型的内容？（比如：活动页面、数据展示报表、表单问卷、管理后台界面等）
数据展示报表，图表，地图，它们都是自定义的amis-editor组件，但是它们的入口不是在amis可视化编辑器的“自定义组件里面”，地图是作为”布局容器“下面的一个新增的地图容器。列表表格是在“数据容器”下的表格里面。图表是在“展示”下的图表里（替换原来的图表组件）
这些自定义组件（地图容器、表格、图表）的数据来源是什么？是通过 API 接口动态获取数据，还是运营人员在编辑器中直接配置静态数据，或者两种方式都需要支持？
地图是通过已有的js sdk提供的方法，前端生成的。表格和图表先通过api接口动态获取数据，后面再添加其他适配。加editor-right-panel上添加一个属性tab，通过选的方式，然后填充到后面的具体配置。相当于我们做一些模版配置。
明白了！在右侧属性面板添加一个 tab，让运营人员通过下拉选择等方式快速填充配置模板。
问题 4/12：
关于这些模板配置的数据源：这些可选的模板配置项（比如图表类型、数据接口选项等）是固定的，还是需要从后端动态获取？如果需要动态获取，你们是否已经有相关的管理接口？
暂时存储在前端静态文件中，结构稳定后，再来进行接口配置完善。
你们现在是否已经有这些自定义组件（地图、表格、图表）的实现代码了，只是需要集成到 amis-editor 中？还是需要从零开始开发这些组件？
我们现在还没有这些自定义组件的实现代码，需要从零开始开发这些组件。
先说地图容器：你们希望使用哪个地图 SDK（比如：高德地图、百度地图、Mapbox 等）？地图上需要展示什么类型的数据（比如：标记点、热力图、区域着色、路线轨迹等）
使用的是公司内部提供的地图SDK，它会把一些属性名以WebG开头的属性挂载到window对象上，供前端调用，这个你先写一个demo，后续我到内网完善
关于表格组件：除了基本的数据展示外，你们需要哪些特殊功能？比如：
行内编辑
批量操作
数据导出
自定义列渲染
分页/虚拟滚动
还是说先实现基础的数据展示表格就可以？
行内编辑
批量操作
数据导出
自定义列渲染
分页/虚拟滚动
都有的
关于图表组件：你们需要支持哪些图表类型？比如：
基础图表（折线图、柱状图、饼图）
复杂图表（雷达图、散点图、热力图、K线图）
地理坐标系图表
3D图表
以及你们倾向于使用哪个图表库（如：ECharts、AntV G2、Chart.js 等）？
先支持基础图表，后续再其他复杂等图表的拓展，因为amis采用的是echarts，所以我倾向于使用echarts
你们对这个编辑器的产出有什么要求？运营人员编辑完成后，是要导出 JSON Schema，还是需要直接生成可部署的页面？如果是生成页面，需要部署到什么环境？
生成页面（就是将编辑好的页面的json存储到数据库），然后根据菜单id去找到这个json，复现页面。
关于页面渲染端：你们是计划在同一个项目里同时包含编辑器和渲染器，还是分成两个独立项目（一个编辑器项目，一个渲染展示项目）？如果分开，渲染端使用什么技术栈？
我们计划在同一个项目里同时包含编辑器和渲染器
关于项目启动和约束条件：
浏览器兼容性要求（比如是否需要支持 IE，或者只需要支持现代浏览器）？
是否有特殊的部署环境限制（比如内网环境、特定的服务器配置等）？
只需要支持现代浏览器。部署到内网环境。