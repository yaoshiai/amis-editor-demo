# 图表类型配置映射关系

## 快速创建 Tab - 配置项显示规则

### 根据图表类型动态显示的配置项

| 配置项 | 折线图 | 饼图 | 柱状图 | 说明 |
|--------|--------|------|--------|------|
| 📊 图表类型 | ✅ | ✅ | ✅ | 所有类型都显示 |
| 🏢 组织机构 | ❌ | ✅ | ❌ | 仅饼图显示 |
| ⏰ 时间维度 | | | | |
|  - 统计周期 | ❌ | ❌ | ✅ | 仅柱状图显示 |
|  - 自动刷新 | ✅ | ✅ | ✅ | 所有类型都显示 |
| 📈 监测指标 | ✅ | ❌ | ❌ | 仅折线图显示 |
| ⚠️ 告警设置 | ✅ | ❌ | ❌ | 仅折线图显示 |
| 快速操作按钮 | ✅ | ✅ | ✅ | 所有类型都显示 |

---

## 各图表类型的详细配置

### 1️⃣ 折线图 (line)

**适用场景**：实时监测、趋势分析

**显示配置**：
- ✅ 图表类型
- ✅ 自动刷新
- ✅ 监测指标（多选：有功功率、无功功率、电压、电流、功率因数、用电量）
- ✅ 小数位数
- ✅ 告警设置（上限/下限阈值）

**数据联动**：
- 选择不同指标 → 更新 Y 轴单位（MW/kV/A/MVar/kWh）
- 多指标 → 显示多条折线
- 告警规则 → 在图表上显示 markLine（告警线）

**典型应用**：
- 24小时负荷监测曲线
- 电压趋势监控
- 多指标对比分析

---

### 2️⃣ 饼图 (pie)

**适用场景**：占比分析、能耗分布

**显示配置**：
- ✅ 图表类型
- ✅ 自动刷新
- ✅ 组织机构（选择部门或变电站）

**数据联动**：
- 选择部门 → 显示该部门下属区域的用电量占比
  - 示例：选择"500kV站A" → 显示其下属设备的用电量分布

**典型应用**：
- 各部门用电量占比
- 变电站能耗分布
- 设备用电结构分析

---

### 3️⃣ 柱状图 (bar)

**适用场景**：数据对比、排名统计

**显示配置**：
- ✅ 图表类型
- ✅ 自动刷新
- ✅ 统计周期（1天/1月/季度对比）

**数据联动**：
- 选择统计周期 → 更新 X 轴数据
  - 1天 → 显示24小时对比
  - 1月 → 显示30天对比
  - 1季度 → 显示月度对比

**典型应用**：
- 各变电站月用电量对比
- 日用电量排名
- 季度用电统计

---

## 自动刷新配置

### 所有图表类型通用

**配置选项**：
- 不自动刷新（手动）
- 5秒
- 10秒
- 30秒
- 1分钟
- 5分钟
- 15分钟
- 30分钟

**实现方式**：
```typescript
// 通过 schema 的 interval 属性控制
{
  interval: 5000,  // 5秒
  api: {
    url: '/api/power/realtime',
    method: 'GET'
  }
}
```

**数据刷新流程**：
1. 用户选择刷新频率
2. amis 自动启动定时器
3. 每隔指定时间调用 API
4. 更新图表数据
5. 重新渲染图表

---

## 数据联动示例

### 示例 1：折线图 - 切换监测指标

**用户操作**：
1. 选择"有功功率" + "无功功率"
2. 设置小数位数为 2

**数据变化**：
```javascript
// 更新前
series: [
  { name: '有功功率', data: [...], unit: 'MW' }
]

// 更新后
series: [
  { name: '有功功率', data: [...], unit: 'MW' },
  { name: '无功功率', data: [...], unit: 'MVar' }
]

// Y轴格式化
yAxis: {
  axisLabel: {
    formatter: (value) => value.toFixed(2) + ' MW/MVar'
  }
}
```

---

### 示例 2：饼图 - 切换部门

**用户操作**：
1. 选择"500kV站A"

**数据变化**：
```javascript
// 更新前（默认数据）
data: [
  { value: 24580, name: '生产车间' },
  { value: 18240, name: '办公区域' },
  ...
]

// 更新后（500kV站A的设备分布）
data: [
  { value: 45230, name: '1#主变' },
  { value: 38650, name: '2#主变' },
  { value: 28940, name: '母线' },
  ...
]
```

---

### 示例 3：柱状图 - 切换统计周期

**用户操作**：
1. 选择"1月"（月度对比）

**数据变化**：
```javascript
// 更新前（1天）
xAxis: {
  data: ['00:00', '02:00', '04:00', ..., '22:00']
}

// 更新后（1月）
xAxis: {
  data: ['1月', '2月', '3月', ..., '12月']
}

// 数据也相应更新为月度数据
series: [{
  data: [12450, 11890, 13240, ..., 12980]  // 月度用电量
}]
```

---

### 示例 4：折线图 - 设置告警

**用户操作**：
1. 添加上限告警：700 MW
2. 添加下限告警：300 MW

**数据变化**：
```javascript
// 更新前
series: [
  {
    name: '有功功率',
    data: [...]
  }
]

// 更新后（添加告警线）
series: [
  {
    name: '有功功率',
    data: [...],
    markLine: {
      data: [
        { type: 'average', name: '平均值' },
        { yAxis: 700, name: '上限告警', lineStyle: { color: '#ee6666' } },
        { yAxis: 300, name: '下限告警', lineStyle: { color: '#91cc75' } }
      ]
    }
  }
]
```

---

## 条件显示实现

### visibleOn 表达式

```typescript
// 仅在折线图时显示
visibleOn: 'this.chartType === "line"'

// 仅在饼图时显示
visibleOn: 'this.chartType === "pie"'

// 仅在柱状图时显示
visibleOn: 'this.chartType === "bar"'

// 所有图表类型都显示（不设置 visibleOn）
```

### 图表类型值

```typescript
// schema.chartType 的值
'line'  // 折线图
'pie'   // 饼图
'bar'   // 柱状图
```

---

## 配置优先级

### 快速创建 Tab vs 属性 Tab

**快速创建 Tab 配置**（优先级高）：
- 业务导向的配置
- 简化的选项
- 预设的默认值
- 条件显示

**属性 Tab 配置**（优先级低）：
- 技术导向的配置
- 完整的选项
- 手动输入
- 始终显示

**冲突处理**：
- 相同字段：快速创建 Tab 的值覆盖属性 Tab
- 不同字段：两处配置合并
- 建议用户主要使用快速创建 Tab

---

## 后续扩展建议

### 短期优化
1. ✅ 条件显示逻辑
2. ⏳ 数据联动实现
3. ⏳ 配置持久化（localStorage）
4. ⏳ 配置验证（如告警阈值合理性）

### 中期优化
1. ⏳ 模板保存/加载
2. ⏳ API 接口集成
3. ⏳ 实时数据刷新
4. ⏳ 数据缓存策略

### 长期优化
1. ⏳ 智能推荐（根据场景推荐配置）
2. ⏳ 配置导入/导出
3. ⏳ 多语言支持
4. ⏳ 配置版本管理
